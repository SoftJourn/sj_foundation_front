import * as types from '../../ActionTypes';

const initialData = {};

export default function project (state = initialData, action) {
    switch(action.type) {
        default:
            return state;
    }
}