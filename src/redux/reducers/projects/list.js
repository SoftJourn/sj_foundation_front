import * as types from '../../../ActionTypes';

const initialData = {};

export default function user (state = initialData, action) {
    switch(action.type) {
        default:
            return state;
    }
}